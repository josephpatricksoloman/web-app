pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                script {
                    def branchName = "${env.GIT_BRANCH}".replace("origin/", "")
                    echo "Current branch: ${branchName}"  // Print the branch name
                    git branch: branchName, url: 'https://github.com/josephpatricksoloman/web-app.git'
                }
            }
        }

        stage('Build') {
            steps {
                script {
                    sh 'bash build.sh'
                    sh 'docker build -t web .'
                }
            }
        }

        stage('Push to Docker Hub') {
            steps {
                script {
                    // Adjust the branch name check to match the actual branch name
                    def imageTag = "josephpatricksoloman/${branchName == 'dev' ? 'dev' : 'prod'}:latest"
                    echo "Pushing image with tag: ${imageTag}"  // Print the image tag being pushed
                    sh "docker tag web ${imageTag}"
                    
                    withCredentials([usernamePassword(credentialsId: 'jose_dockerhub', passwordVariable: 'DOCKER_PASSWORD', usernameVariable: 'DOCKER_USERNAME')]) {
                        sh "echo \$DOCKER_PASSWORD | docker login -u \$DOCKER_USERNAME --password-stdin"
                        sh "docker push ${imageTag}"
                    }
                }
            }
        }
    }
}
